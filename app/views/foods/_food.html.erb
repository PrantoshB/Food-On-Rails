<tr id="<%= dom_id food %>">
    <td><%= food.name %></td>
    <td><%= food.measurement_unit %></td>
    <td><%= food.price %></td>
    <td>
      <%= link_to "Remove", food, method: :delete, class: "btn-text color-red" %>
    </td>

</tr>
